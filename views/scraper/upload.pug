extends ../layout

block content
  div(class="container")
    div(class="row")
      h2 Upload Scraped File

      div(class="well well-md")
        p Uploaded file must be in tabular format (.xlsx .xls or .csv) and must include the following column headers (all lowercase):
        ul
          li name (article name)
          li article_uri
          li publisher
          li journal
          li date (issue date)
          li volume (issue volume)
          li number (issue number)

        p Column order is not important and additional columns will be ignored.

      form(method="POST" enctype="multipart/form-data")
        input(type="file" name="data" id="data" class="hidden")
        button(type="button" class="btn btn-primary" id="file-button" name="file-button") Choose File
  script(src="/js/scraper-upload.js")
